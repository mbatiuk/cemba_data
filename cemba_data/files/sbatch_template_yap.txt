#!/bin/bash
#
#
#   Copy/edit this script as desired.  Launch script with sbatch on a login node.
#    
#   Default value of OMP_NUM_THREADS is 1; be sure to change it! Match with --cpus-per-task
#
#   Increase thread count gradually while looking for optimal setting.
#  
#
#----------------------------------------------------

#SBATCH -J {job_name}                     # Job name
#SBATCH -o {log_dir}/{job_name}.o%j       # Name of stdout output file
#SBATCH -e {log_dir}/{job_name}.e%j       # Name of stderr error file
#SBATCH --qos serial                      # Modify depending on HPC node type
#SBATCH --mem 300G                        # Memory usage, originally was 100G, can be decreased from 300G
#SBATCH --cpus-per-task 62                # Modify depending on the node type
#SBATCH -N 1                              # Total # of nodes
#SBATCH -n 1                              # Total # of mpi tasks
#SBATCH -t {time_str}                     # Run time (hh:mm:ss)
{email_str}
{email_type_str}


# Some information

echo
echo STARTING AT `date`
echo

pwd



# Prepare environment
module purge

# Modify according to your conda installation type (conda, mamba, etc)
source $HOME/mambaforge/etc/profile.d/conda.sh

# Activate conda yap mapping environment, making sure name is correct
conda activate yap


# Set thread count (default value is 1)... Match this with --cpus-per-task value in the header
export OMP_NUM_THREADS=62


# Actual command

{command}




# Deactivate environment

conda deactivate


# Print info about end of run

echo
echo STOPPING AT `date`
echo
